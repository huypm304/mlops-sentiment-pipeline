# Pin versions compatible with SageMaker PyTorch 2.1 inference DLC (py310).
# Do NOT install torch here — the container already provides torch 2.1.0+cpu.
transformers>=4.36.0,<4.46.0
tokenizers>=0.15.0,<0.20.0
pytorch-crf>=0.7.2
numpy>=1.24.0,<2.0.0
safetensors>=0.4.0,<0.5.0
